# aspire-network

docker run --rm -v keycloak-data:/opt/keycloak/data -v D:\code\realms:/opt/keycloak/export quay.io/keycloak/keycloak:26.3 export --realm network --dir /opt/keycloak/export --users realm_file

![alt text for screen readers](/sodo.png "Text to show on mouseover")
